# === PostgreSQL (WorkHub) ===
# Standard PostgreSQL Database URL (e.g. postgresql://user:password@host:port/dbname)
DATABASE_URL=postgresql://workhub_app:ChangeMe_S3cure!@localhost:5432/workhub_db
HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# Render/Cloud port (Spring uses server.port=${PORT:8080})
# PORT=8080
